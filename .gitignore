__pycache__/
*.pyc
.pytest_cache/
*.sqlite3-shm
*.sqlite3-wal


# Excel/CSV normativo retirado: la fuente primaria es la ontologia (data/ontologia)
data/raw/NORM_17_06.csv

# Backups de los PDF que genera actualizar_fuentes.py
*.bak

# Ficheros temporales de bloqueo de Office (aparecen al abrir un .pptx/.xlsx/.docx)
~$*

# Claves locales (usar .env.example como plantilla)
.env

# Certificado TLS autofirmado (se genera en cada equipo con generar_certificado.py;
# la clave privada NUNCA se versiona)
cert.pem
key.pem

# Config local de Claude Code (no es del proyecto)
.claude/
