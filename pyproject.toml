[tool.poetry]
name = "gas-quality-comparator"
version = "0.1.0"
description = "Comparador normativo gasista ES/EU para O2, H2S y PCS"

[tool.poetry.dependencies]
python = "^3.11"
pdfplumber = "^0.11.7"

[tool.poetry.dev-dependencies]
pytest = "^7.0"
