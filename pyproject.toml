[tool.poetry]
name = "gas-quality-comparator"
version = "0.1.0"
description = "Comparador regulatorio de calidad de gas natural en Europa (ES · PT · FR · UE), 10 parámetros, con trazabilidad y motor determinista"

[tool.poetry.dependencies]
python = "^3.11"
pdfplumber = "^0.11.7"

[tool.poetry.dev-dependencies]
pytest = "^7.0"
